# GENERATED FILE — do not edit by hand.
# Contract: e12974b8070030b149ca23edfbf751fe4720b50d
# Regenerate with:
#   scripts/gen-container-candidate.sh workflow e12974b8070030b149ca23edfbf751fe4720b50d container-candidate.json > .github/workflows/container-candidate.yml
name: immutable container candidate

on:
  push:
    branches: [main]
  pull_request:

permissions:
  attestations: write
  contents: read
  packages: write
  id-token: write

jobs:
  candidate:
    uses: Verjson/.github/.github/workflows/container-candidate.yml@e12974b8070030b149ca23edfbf751fe4720b50d
    with:
      config-path: container-candidate.json
      contract-ref: e12974b8070030b149ca23edfbf751fe4720b50d
      acquisition-sha256: 198f7ddd7e628b678f6225c9f22039c5bfc49c1599d9ca169cc15130a3001ab0
    secrets:
      NODE_AUTH_TOKEN: ${{ secrets.NODE_AUTH_TOKEN }}
