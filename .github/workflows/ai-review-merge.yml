# GENERATED FILE — do not edit by hand.
# Regenerate with:
#   scripts/gen-ai-review-caller.sh 63fc49c68e46c1915bdc07db29d68f3f76d4377e > .github/workflows/ai-review-merge.yml
name: AI review

on:
  workflow_dispatch:
    inputs:
      pr_number:
        required: true
        type: string
      expected_head_sha:
        required: true
        type: string
      authorization_check_id:
        required: true
        type: string
      arm_run_id:
        required: true
        type: string
      arm_run_attempt:
        required: true
        type: string
      explicit_rereview:
        required: false
        type: boolean
        default: false
      review_policy:
        required: true
        type: string

permissions:
  actions: write
  checks: read
  contents: read
  issues: write
  pull-requests: write
  statuses: read

jobs:
  review:
    uses: Verjson/.github/.github/workflows/ai-review-merge.yml@63fc49c68e46c1915bdc07db29d68f3f76d4377e
    secrets:
      AI_REVIEW_APP_PRIVATE_KEY: ${{ secrets.AI_REVIEW_APP_PRIVATE_KEY }}
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
    with:
      pr_number: ${{ inputs.pr_number }}
      expected_head_sha: ${{ inputs.expected_head_sha }}
      authorization_check_id: ${{ inputs.authorization_check_id }}
      arm_run_id: ${{ inputs.arm_run_id }}
      arm_run_attempt: ${{ inputs.arm_run_attempt }}
      explicit_rereview: ${{ inputs.explicit_rereview }}
      review_policy: ${{ inputs.review_policy }}
