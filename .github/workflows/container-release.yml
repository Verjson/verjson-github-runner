# Generated by Verjson/.github/scripts/gen-container-release.sh; do not edit.
name: container release
on:
  workflow_dispatch:
    inputs:
      version: {required: true, type: string}
      candidate-manifest: {required: true, type: string}
permissions:
  actions: read
  attestations: write
  contents: read
  id-token: write
jobs:
  release:
    uses: Verjson/.github/.github/workflows/container-release.yml@b4b5cb7e8ccf9eede35516eafbbe62179042254d
    with:
      version: ${{ inputs.version }}
      candidate-manifest: ${{ inputs.candidate-manifest }}
      config-path: container-candidate.json
      contract-ref: b4b5cb7e8ccf9eede35516eafbbe62179042254d
    secrets:
      release-token: ${{ secrets.RELEASE_TOKEN }}
