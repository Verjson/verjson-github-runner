# Generated by Verjson/.github/scripts/gen-container-release.sh; do not edit.
name: container release
on:
  workflow_dispatch:
    inputs:
      version: {required: true, type: string}
      candidate-manifest: {required: true, type: string}
permissions:
  actions: read
  attestations: write
  contents: read
  id-token: write
  packages: write
jobs:
  release:
    uses: Verjson/.github/.github/workflows/container-release.yml@da8a2154b6a3d2109d48cfbd82e153df8a9dedc9
    with:
      version: ${{ inputs.version }}
      candidate-manifest: ${{ inputs.candidate-manifest }}
      config-path: container-candidate.json
      contract-ref: da8a2154b6a3d2109d48cfbd82e153df8a9dedc9
      release_app_client_id: ${{ vars.RELEASE_APP_CLIENT_ID }}
    secrets:
      release_app_private_key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}
