# Generated by Verjson/.github/scripts/gen-container-release.sh; do not edit.
name: container release
on:
  workflow_dispatch:
    inputs:
      version: {required: true, type: string}
      candidate-manifest: {required: true, type: string}
permissions:
  actions: read
  attestations: write
  contents: read
  id-token: write
jobs:
  release:
    uses: Verjson/.github/.github/workflows/container-release.yml@e12974b8070030b149ca23edfbf751fe4720b50d
    with:
      version: ${{ inputs.version }}
      candidate-manifest: ${{ inputs.candidate-manifest }}
      config-path: container-candidate.json
      contract-ref: e12974b8070030b149ca23edfbf751fe4720b50d
    secrets:
      release-token: ${{ secrets.VERJSON_RELEASE_TOKEN }}
